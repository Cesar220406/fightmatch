# ── Dominio ────────────────────────────────────────────────────────────────
# Tu dominio (sin https://)
DOMAIN=fightmatch.tudominio.com

# ── Base de datos ──────────────────────────────────────────────────────────
POSTGRES_USER=fightmatch
POSTGRES_PASSWORD=CAMBIA_ESTO_POR_ALGO_SEGURO
POSTGRES_DB=fightmatch

# ── JWT ────────────────────────────────────────────────────────────────────
# Genera con: openssl rand -base64 64
JWT_SECRET=CAMBIA_ESTO_POR_UN_SECRETO_LARGO_Y_ALEATORIO
JWT_EXPIRES_IN=7d
